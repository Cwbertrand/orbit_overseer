import styled from 'styled-components/native';

export const LobbyWrapper = styled.View`
  height: 100%;
  position: relative;
`;

export const NavButton = styled.View`
    flex: 1;
    align-items: center;
    justify-content: flex-end;
    margin: 0 20px 10% 20px;
`;